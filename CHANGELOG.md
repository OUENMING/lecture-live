# Changelog

本文件记录 ClassLive 的显著变更。
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

> **本项目没有 pip 包，安装方式就是 `git clone`** —— 所以升级 = `git pull`。
> 每次 `pull` 后请对照本文档确认要不要补依赖或模型（见各版本的「升级须知」）。

### 每个版本请写一节 `### 更新看点`

那几行**就是更新卡片上显示的内容**，由 `main._changelog_summary` 读出来。
读不到才退回去抓 `### Added` / `### Fixed` —— 那些是写给维护者的（含文件名、
符号名、代码片段），抓出来上卡片没人看得懂。

写法（2026-09-24 调研 Keep a Changelog 1.1.0 + 各家 "What's New" 惯例）：

1. **写产品本身做了什么** —— scrim 变了、半截词不再裂行、孤儿图标修了。
   ⚠️ **不要写弹窗/更新机制自己的设置**（"新增了说明卡片""卡片改了定位算法"）——
   那是工具的工具，用户不关心，而且一个刚升级的人也撞不到刚引入的 bug。
2. **破坏性变更排最前**，并在行首带 `⚠️`（合并多版本时会据此提到最前）。
3. **分类标签沿用 `Added` / `Changed` / `Fixed`** —— 行业标准，不译为中文。
4. **一条一事**，不把无关改动捆一行。
5. **带实测数字**（`0.306 → 0.345` 而不是"透明感更好"）。
6. **该专业的地方必须专业，其余地方可以有幽默感。**
   破坏性变更 / 数据丢失 / 安全相关的条目 —— 一句话说清、不带修饰、不玩梗。
   其余条目可以放松：加一句小趣味、自嘲、顺手记一笔生活（作者的风格是
   **小趣味、不油腻、不硬凹** —— "还有吃了碗麻辣烫，美味😋" 这种可以；
   玩梗玩到读者看不懂就不行）。
7. **每个版本的看点第一行放一句节日问候**（有节日时）—— 放最前，别埋在后面。
   写**「中秋快乐」不是「中秋安康」**：后者源自 2015 年一篇自媒体讹传
   （假托"非遗专家杨广宇"，**查无此人**；上海辟谣平台 + 北大张颐武、
   华东师大田兆元均已辟谣）。而且中秋是**团圆节**，传统上就是祝"快乐"。

⚠️ 分类标签与实测数字是**专业区**，照旧；语气是**可以放松区**。两者不冲突。
⚠️ 本项目读者是开发者（测试阶段），所以刻意保留符号名与实测值；
将来若面向非开发者分发，改各版本的这一节即可。

> 💡 每行**不必**以 `- ` 开头（解析器容错），但写了更整齐 —— 漏写不会被丢掉了。
> 💡 **小节顺序不用管** —— 卡片里由 `_reorder_log` 统一把「更新看点」提到最前、
> 「升级须知」紧随其后，其余小节保持你写的相对顺序。你按什么顺序写都行。

### 每个版本可选写一节 `### 更新方式`

只有一行：`auto` 或 `manual`（或干脆不写）。它决定这个版本能不能**后台自动更新**。

- **`auto`** —— 退出时自动拉，用户零操作。只该用在**没有依赖变化、不要求新模型、
  没有破坏性变更**的版本上。
- **不写 / `manual`** —— 要用户自己点。**这是默认值**（默认拒绝：没声明自己安全的，
  绝不自动应用）。

⚠️ **为什么不用版本号判**：本仓库 `3.4.0 → 3.5.0` 是 **minor** 版本号、内容却是
**breaking**（两个 ASR 模型改必装、缺了启动即报错）。作者不按 semver 发版，
按版本号分级会正好漏掉最危险的那些。

### 跨版本升级会合并显示

有人会从 3.4.0 直接跳到 3.6.0。那种情况下卡片会把**中间的版本一起报**
（`_whatsnew_body` 从最新往下数到「你上次看过的版本」为止），多版本时每行前面标版本号。
所以**每个版本的看点都要能独立看懂**，不要写"同上""见上一条"。

## [3.6.3] - 2026-09-24

### 更新方式

auto

### 更新看点

- 🌕 **中秋快乐** —— 希望大家周五不用上课
- ⚠️ 这次还是要手动更新一次 —— 3.6.2 里还没有自动更新机制，神仙也变不出来
- **Added** 更新卡片上多了「立即更新」按钮和一个状态行 —— 点一下就拉，不用开终端
- **Added** 以后的小更新会**在你关掉工具之后自动拉**（后台子进程，不等你）
- **Changed** `cl update` 与卡片按钮现在**共用同一份实现**（`update.py`），规则不会再两边漂移

### 升级须知（3.6.2 → 3.6.3）

> ⚠️ **这一次必须手动。** 自动更新机制本身是 3.6.3 才加进来的 ——
> 你手上的 3.6.2 里没有它，所以它没法把自己变出来（和 3.4.0 不认识 `update`
> 那次是同一种"鸡生蛋"）。**跑完这一次，以后的小更新就不用管了。**

```bash
cd 你的 lecture-live 目录 && cl update
```

**不需要补依赖，也不需要动模型。** 本次 `requirements.txt` **没有变化**。

### Added
- **`update.py`** —— 更新核心，被**两条路径共用**：`cl update` 与卡片上的
  「立即更新」按钮。抽出来的理由：安全规则（脏工作区停手 / `--ff-only` / 不下模型）
  两边各写一份迟早在某一边漂移，而错一条就是丢用户的工作。
- **卡片底部加「立即更新」按钮 + 常驻状态行**。状态行是必需的：卡片原本只在
  `VERSION` 变了时弹（= 已经拉完了），那一刻没什么可拉的，按钮会显得没意义。
  现在状态行常驻显示当前版本 / 更新结果。
- **退出时的自动更新（`--auto`）**，只对声明了 `### 更新方式 auto` 的版本生效。
  设计依据（调研见 `docs/PLAN-update-mechanism.md` §7）：
  - **跑在独立子进程里**（`start_new_session`）—— Mozilla 把
    "We don't want to delay shutdown" 明确列为设计目标；atomic 的原话是
    "the parent process never touches the network"。父进程只 `Popen` 一下就返回，
    **零退出延迟**（同步跑的话断网时要干等 45 秒才关得掉）。
  - **限频一小时一次** + **并发锁（陈旧锁 15 分钟自过期）** + **日志落盘**
    （`~/Library/Logs/ClassLive/update.log`；静默失败没日志就没法排查）。

### Fixed
- `update.remote_file` 原本**硬编码 `origin/main`** —— 远程名或分支名与作者不同
  （改过名的 fork、`master` 分支、镜像）就整个失效。改为先解析 `@{u}`。
- `overlay.py` **从来没有 `import os`**，而新加的更新回调用了 `os.environ` ——
  点按钮会 `NameError`，线程静默死掉、按钮永远卡在「更新中…」。
  同时给整条线程加了 `finally`，保证任何**没预料到**的异常都不会把按钮锁死。

## [3.6.2] - 2026-09-24

### 升级须知（3.6.1 → 3.6.2）

**不需要补依赖，也不需要动模型。** 直接：

```bash
cd 你的 lecture-live 目录 && cl update
```

### 更新看点

- 🌕 **中秋快乐** —— 希望大家周五不用上课
- **Fixed** 更新看点里漏写 `- ` 开头的行会被**静默丢掉**（现在容错了）

### Fixed
- `_changelog_summary`：「更新看点」小节里的行原本**必须**以 `- ` 或 `* ` 开头，
  否则**静默跳过** —— 手写时漏了前缀，那几行就不上卡片，而且**没有任何提示**。
  改成除空行 / 小标题 / 引用 / 代码块外，一律当条目。
- `CHANGELOG.md`：补回三处漏写的 `- ` 前缀（3.6.1 / 3.6.0 / 3.5.0 各一处），
  并修掉一处断在半句的逗号。

### Changed
- 把「更新日志怎么写」明确一条风格：**该专业的地方专业**（破坏性变更 / 数据丢失 /
  安全相关必须直说、不带修饰），**其余地方可以有幽默感** —— 小趣味、不油腻、不硬凹。

## [3.6.1] - 2026-09-24

### 升级须知（3.6.0 → 3.6.1）

**不需要补依赖，也不需要动模型。** 直接：

```bash
cd 你的 lecture-live 目录 && cl update
```

### 更新看点

- **Fixed** 跨版本升级会漏报中间版本 —— 从 3.4.0 直接跳到 3.6.0 的人看不到 3.5.0 的破坏性变更
- **Fixed** 一些小修小改
- **Nothing** 还有吃了碗麻辣烫，美味😋

### Fixed
- `_whatsnew_body`：卡片内容原本只取 `VERSION` 那**一个**版本。跨版本升级时
  （例如 3.4.0 → 3.6.0）中间的版本说明会**整段漏掉** —— 包括 3.5.0 那条
  「两个 ASR 模型改为必装、第一次要手动 `git pull`」，恰恰是跳过版本的人
  最需要看到的一条。改为从最新往下数到 `.update-seen` 为止，全部合并；
  多版本时每行标版本号，并把带 `⚠️` 的提到最前（稳定排序，其余保持新版本在前）。
- `_changelog_date`：用 `rf"…\d{4}…"` 写正则，f-string 会把 `{4}` 当**表达式**求值 →
  模式变成 `(\d4-\d2-\d2)` → 编译通过、**永不匹配、静默返回空串**。改成字符串拼接。

### Changed
- 卡片的「更新看点」清掉自我描述的行（"新增了说明卡片"之类）—— 只留产品本身的变化。

## [3.6.0] - 2026-09-24

### 升级须知（3.5.0 → 3.6.0）

**不需要补依赖，也不需要动模型。** 直接：

```bash
cd 你的 lecture-live 目录 && cl update
```

### 更新看点

- **Changed** 面板 scrim `0.45` → `0.38` —— 透射率 0.306 → 0.345，白底对比度 2.60 → 2.33:1
- **Fixed** 流式重折把半截词裂成两行（`_answer_refold` 的 flush 语义）
- **Fixed** 状态项创建失败会留清不掉的孤儿菜单栏图标
- **Fixed** 一些小修小改，性能优化

### Added
- **更新说明卡片**（`whatsnew.py`）：更新到新版本后弹出，每个版本只弹一次；
  可在卡片上勾「以后不再提示」。**非模态**——它只是浮在字幕面板旁边，不理它也照样
  开始转录。不用 `NSAlert` 的三条理由（含 Apple HIG 原文）写在 `whatsnew.py` 文件头。
- 更新卡片的内容由本文件的「更新看点」小节驱动，见上方说明。

### Fixed
- **`whatsnew.py` 的 ObjC 类名与 `overlay.py` 撞车** —— 两边都定义了 `_Panel` /
  `_DragLayer`，ObjC 运行时按名字全局注册，第二个定义抛
  `_Panel is overriding existing Objective-C class`，被 fail-soft `except` 吞掉，
  卡片**静默不显示**。独立测试全绿是因为那时 overlay 没加载，**必须同进程才复现**。
  ⚠️ 隐患比表面大：**谁先注册谁赢**，顺序一反过来崩的是 overlay 本体。已加前缀。
- **卡片定位越界被 AppKit 夹回** —— 原逻辑只算一个位置就落位，算出面板正上方
  `y=822`，但屏幕可见区顶只有 944，AppKit 把越界窗口夹回（944-273=671），
  卡片掉下来**盖住字幕面板**。改成先判断放得下再落位（左/右/下/上依次试）。
- **卡片上出现字面的 `**`** —— 摘要从本文件抠出来是 Markdown，而卡片只画纯文本。
- `overlay.py`：流式回答重折（拖窗口改宽度触发）会把**还在流式中的半截词**当成
  已闭合提交，屏上裂成两行。改用 `flush=self._answer_finished`。
- `overlay.py`：状态项创建在 `statusItemWithLength_` **之后**失败时会留**清不掉的
  孤儿菜单栏图标**（引用已置 None，`close()` 再也 remove 不到）。改为先 remove 再置 None。

### Changed
- 面板 scrim `0.45` → `0.38`（透明感调强一档）。用**黑底/白底双拍**实测：
  透射率 0.306 → 0.345，白底白字对比度 2.60 → 2.33:1（可读性由字形描边承担，
  已实拍纯白 PPT 确认清晰）。⚠️ 代码注释里原来的「a=0.45 → 3.33:1」是**旧配方**
  下测的数，与本次实测对不上，已标注作废。
- 删掉 `overlay.py` 两个死字段 `_translating` / `_correcting`。

## [3.5.0] - 2026-09-24

### 更新看点

- ⚠️ **Breaking** 两个 ASR 模型改为**必装**，取消静默回退；**第一次更新要手动 `git pull`**（旧版 `cl` 还不认识 `update` 子命令）
- **Added** `cl test` —— 跑一节真实课采集全量指标，一键打包发作者
- **Added** `cl update` —— 一条命令更新（拉代码 + 按需补依赖 + 自检）
- **Added** `cl doctor` —— 只读自检，缺什么告诉你跑哪条命令
- **Fixed** 笔记写入可能丢数据（改原子写；备份失败不再静默）
- **Fixed** 术语分类不再无条件降级 basic（避免不可逆的错位状态留在库里）
- **Nothing** 不想上学

### Added
- **`cl test` 测试模式** —— 跑一节真实课，采全量指标（逐段 ASR 耗时/电平/波峰因数/
  置信度/用了哪个模型/是否回退 + 资源占用 + VAD 诊断），收尾打成一个 `bundle.zip`
  方便发给作者。默认录音频（约 28MB/15 分钟）；`--no-record-audio` 只留 ~10KB 的纯指标包。
  ⚠️ 含课堂音频与逐字转录，可能含其他同学的声音 —— 启动与收尾都会提醒。
- **`cl update`** —— 一条命令更新：拉代码 + **按需**补依赖 + 自检。三条边界：
  工作区有本地改动时**停手**（绝不 stash/丢弃）；`--ff-only`（分叉时响亮失败，
  不静默造 merge）；**绝不自动下模型**（只打印命令）。

### ⚠️ Breaking changes
- **两个 ASR 模型都改为必需**，取消静默回退。此前 `whisper-turbo` 缺失会警告后
  回退 Parakeet（"能用但少一档质量"）；**现在缺任何一个都在启动时报错并退出**。
  理由：定稿模型是转写质量的主要来源（实测**有效词数 +44%**，8/8 窗口一致），
  静默少掉它是"看着正常但打了折"。**升级后请跑 `cl doctor` 或 `cl update`。**

### Fixed
- `translator` 懒加载的**真竞态**：`self._model, self._tokenizer = load(...)` 是两条
  STORE_ATTR 且 model 在前，另一线程可能恰在两条之间读到 `_model` 已非 None，
  于是拿着还是 `None` 的 `_tokenizer` 去 `apply_chat_template`。
  **修法：换成 tokenizer 先、model 后** —— `_model` 成为真正的就绪标志。
  （`auto` 模式下模型不预热，**首次本地调用就发生在 worker 线程上**，这是会走到的路径。）
- `cl-bg.py` 默认日志从 `/tmp` 固定路径移到 `~/Library/Logs/ClassLive/`，
  并加 `O_NOFOLLOW`。实测无 `O_NOFOLLOW` 时**符号链接注入是成功的**。
- `rebuild_note.py`：`--vault ""`（或 `OBSIDIAN_VAULT` 设为空串 —— `os.environ.get`
  在变量存在但为空时**不回退默认值**）会走到 `Path(None)` 抛 TypeError。
- `build_notes.py` 两处**数据丢失风险**：
  ① `save_to` 改为**原子写**（.tmp + `os.replace`），且备份失败**不再静默**；
  ② 分类时模型没给出可识别档位不再**无条件降级成 basic** —— 有 `detail` 的按 gloss 处理，
  否则会留下"level=basic 但 detail 是 80–160 字整段"的错位状态，且**永久留在库里**。
- `cache_probe.py`：参数校验（非数字/`n<=0`/无 ASR 行都友好报错）；
  请求数口径拆成 `requests` / `calls`（原来拿"有 usage 的请求数"当总请求数，两个数同时偏小）。
- `probe_scroll.py`：提示标签不再只在开头取一次高度，阶段切换后重算位置。
- `build_notes.py`：`load_raw()` 改显式传参 —— 它的**无参默认值在定义时绑定**，
  而 `save_to(NOTES_FILE)` 是**调用时**取全局，两者一旦不一致就是"读一个写另一个"。

### Changed
- README 路线图按实测重排：把**已经做完并否决**的几件事从待办移走
  （降噪 −41% / 调 VAD 零差异 / 换模型 4 个全败 / 课号匹配 p=0.69 / 热词段错误 / WPE 测不出），
  并新增「为什么不做降噪 / 人声分离 / 频段滤波」一节。
- 删掉产品用不到的依赖与模型：`nara-wpe` / `scipy` / `bottleneck`、`~/models/denoise`。
  逐个核对 17 个 `add_argument`，无死参数。

### 升级须知（3.4.0 → 3.5.0）

> ⚠️ **第一次更新必须先手动 `git pull`** —— 这是"鸡生蛋"：
> **3.4.0 的 `cl` 还不认识 `update` 这个子命令**（实测会报"未知参数: update"）。
> 一个工具没法用自己更新出"能更新的自己"。**跑一次下面的命令之后，`cl update` 就永久可用了。**
>
> ```bash
> cd ~/lecture-live && git pull      # 一次性引导（仓库路径按你的实际位置改）
> ```

1. 引导之后就都能用 **`cl update`** 了（拉代码 + 按需补依赖 + 自检）。
2. 本次 `requirements.txt` **没有变化**，不需要重装依赖。
3. **确认两个 ASR 模型都在**：跑 `cl doctor`；缺哪个它会打印下载命令。
4. ⚠️ 本次是**破坏性变更**：模型不全时会**启动失败**（以前只是警告）。

## [3.4.0] - 2026-09-24

### 更新看点

- **Added** 定稿接入 Whisper-large-v3-turbo，同批真实录音实测**有效词数 +44%**
- **Added** 三档模式：双语 / 只英·校 / **纯转录**（第三档完全不调 LLM，零首字延迟）
- **Added** 悬浮窗可自由缩放（原生四角拖拽），窗口尺寸会记住
- **Added** `requirements.txt` 终于有了 —— 此前依赖只写在 README 里，环境不可复现
- **Changed** 双语行顺序改为**英文在上、中文在下**（依据是三条同行评审研究，见正文）
- **Changed** 翻译开关由 2 态改为 **3 态**（旧的"开"= 新的「只英·校」档）

### Added
- **定稿增强模型**：定稿路径可选接入 Whisper-large-v3-turbo（`--final-model-dir`）。
  同批真实课堂录音实测：有效词数 **+33%**、段尾无终止标点 **22%→14%**、空转写 **9%→2%**。
  草稿路径不变（Whisper 只有 4× 实时，供不上"每秒一份草稿"）。模型缺失会自动回退 Parakeet。

  > ⚠️ **2026-09-24 更正**（3.5.0 时重测）：词数增益实测是 **+44%**（不是 +33%，当时报低了）；
  > 而"段尾无终止标点 22%→14%"的幅度**落在噪声带内**（该指标 n≈11 段时底线 ±18 个百分点），
  > **不足以判定** —— 保留原文不改写当日记录，但别把那个数字当结论。
  > 定性证据（`part`→`pot`、`he jokes`→`heat up`）不受指标噪声影响，仍然成立。
- **三档模式**：悬浮窗顶栏一个按钮循环「双语 / 只英·校 / 纯转录」。
  第三档**完全不调 LLM**（零 API、零首字延迟），补上了此前"不想联网且不要中文"的空缺。
- `requirements.txt` —— 此前依赖只在 README 里手打，环境不可复现。
- `cl doctor` —— 只读自检（版本/依赖/模型/术语表），缺什么打印该跑的命令。
  **不自动下载、不自动安装**。
- 启动时的**一次性**更新提示：落后远程时打印一行，同一版本只提示一次，可用
  `CLASSLIVE_NO_UPDATE_CHECK=1` 关闭。只问本地 git，不发 HTTP。
- `docs/experiments/` —— 9 个可复跑的对照实验脚本（换模型 / 降噪 A/B / VAD 调参 / 缩放探针）。
  README、DESIGN 里的"实测"数字大多能在这里找到对应脚本。
- 悬浮窗可自由缩放：原生四角/四边拖拽、缩放光标、最小 280px 宽 / 1 行高。
- 窗口尺寸记忆（`.window`），下次启动恢复。

### Changed
- **悬浮窗「翻译开关」由 2 态改为 3 态**。旧的两态行为 = 新的「只英·校」档。
  代码层面：`on_translate` 回调的入参从 `bool` 变为 `str`（`"both"` / `"en"` / `"raw"`）。
- **双语行的竖排顺序改为「英文在上、中文在下」**。依据是三条独立同行评审研究：
  双语字幕里 L2 行被系统性略读，而**只有"哪一行在上面"被证实能改变注意分配**
  （不是字重/字号/颜色）。想回退：`transcript_view.EN_LINE_ON_TOP = False`。
- 定稿路径的执行顺序改为**先跑 Whisper、退化才回退 Parakeet**（实测省 14%，因为原来那趟
  Parakeet 绝大多数时候都是白跑的）。

### Fixed
- `obsidian_writer._overview` 用整体替换而非合并 —— 模型漏返回字段时会丢掉第一段已算好的概览。
- `build_notes.TermNotes.add()` 硬编码全局 `AUTO_FILE`，而 `__init__` 按传入 `path` 推导 ——
  自定义路径时读写不是同一个文件（测试里 `TermNotes(临时目录)` 会写坏真实缓存）。
- `asr.py` 三个模型文件各自独立解析，int8 与非 int8 混用不会报错但识别结果"看着正常全错" —— 加一致性校验。
- `cl-bg.py` 把 `argv[1]` 插进 `bash -c` 命令串（命令注入）；日志权限 0644 → 0600。
- `translator._load_terms` 裸读术语表（非 UTF-8 / 权限问题会抛异常）。
- `cloud_translator._stream_chat` 不重置 `last_usage`，空流时会把上一次用量算两遍。
- `.gitignore` 未覆盖 `.env` / `*.key` / `*.pem` / `id_rsa*` 等常见密钥形态。
- `probe_scroll.py` 屏幕提示与判定互相矛盾（断言反转后的遗留）。
- `CLAUDE.md` / `ARCHITECTURE.md` 里"`polish.py`、`tests/` 不在 git 里"的**过期断言** ——
  它们其实早已提交，文档没回头改（已在文档里标明快照日期与现查方式）。

### Removed
- 顶栏「展开」按钮。它与「滚动权限」耦合，而滚动权限已与收起/展开解耦（拉窗口 = 看几句，
  滚动 = 往回翻，两者正交）。`_apply_mode` 保留 —— 答案接管仍在用它。

### 升级须知（3.3 → 3.4）
1. `git pull` 后跑一次 `uv pip install --python .venv/bin/python -r requirements.txt`
   （本次没有新增依赖，但今后以此为准）。
2. 可选：装定稿模型（~1GB），命令见 `cl doctor` 的输出。
3. `on_translate` 回调签名变了 —— **只影响自己改过 `overlay.py` 的人**。

---

## [3.3.0] 及更早

早期版本没有维护 changelog。历史见 `git log`。
